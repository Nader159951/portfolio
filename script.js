// =========================================================
// TRANSLATIONS — edit the "ar" side with your own words.
// Every data-i18n="key" in index.html pulls from here.
// =========================================================
const TRANSLATIONS = {
  "meta.title": {
    en: "Nader Esawiy professional portfolio",
    ar: "نادر عيسوي portfolio المهني"
  },
  "nav.about":   { en: "about",       ar: "نبذة" },
  "nav.skills":  { en: "skills",      ar: "المهارات" },
  "nav.work":    { en: "how i work",  ar: "طريقة العمل" },
  "nav.gallery": { en: "gallery",     ar: "الصور" },
  "nav.contact": { en: "contact",     ar: "تواصل" },

  "hero.name": { en: "Nader Ragab Esawiy", ar: "نادر رجب عيسوي" },
  "hero.role": {
    en: "Full-Stack Developer & Infrastructure Engineer",
    ar: "مطوّر ويب متكامل ومهندس بنية تحتية"
  },
  "hero.desc": {
    en: "I build educational applications, platforms, and integrated operating systems tailored to specific needs, and I specialize in server and infrastructure management.",
    ar: "ببني تطبيقات ومنصات تالتحتيهعليميه وانظمة تشغيل متكامله حسب الاحتياجات ومتخصص في ادارة السيرفارات والبنيه "
  },
  "hero.cta1": { en: "get in touch", ar: "تواصل معايا" },
  "hero.cta2": { en: "see the stack ↓", ar: "شوف المهارات ↓" },

  "about.title": { en: "A brief overview of myself .", ar: "نبذه بسيطه عني ." },
  "about.p1": {
    en: "My name is Nader Ragab Eissawy, and I will be 17 years old in 2026. I am currently working on the development of educational platforms, infrastructure, and operating systems.",
    ar: "انا اسمي نادر رجب عيسوي سني في 2026 هو 17 سنه انا حاليا شغال في تطوير المنصات التعليميه والبنيه التحتيه وانظمة التشغيل "
  },
  "about.p2": {
    en: "I have currently put work on hold because I am in my final year of high school, so I am dedicating all my time to my studies. As for my career, I love programming, and—God willing—it will be my primary line of work.",
    ar: "انا حاليا واقف شغل بسبب اني في الصف الثالث الثانوي العام فمرغ وقتي كله حاليا لي الدراسه فقط ام من نحية شغلي انا بحب شغل البرمجه كله هيبقي الشغل الساسي ليا باذن الله تعالي"
  },
  "about.fact.location": { en: "Qalyubia Governorate, Toukh Center, Mit Kinana Village", ar: "محافظة القليوبيه مركز طوخ قرية ميت كنانه" },
  "about.fact.focus": { en: "Developer of Flutter apps, web applications, and educational platforms; also an infrastructure and network engineer.", ar: "مبرمج فلاتر وتطبيقات ويب ومنصات تعليميه بالاضافه لي مهندس بنية تحتيه وشبكات" },
  "about.fact.currently": { en: "I am currently in my final year of high school; I’m not learning anything new due to my studies, but I am honing my existing skills.", ar: "انا حاليا في الصف الثالث الثانوي متعلمش حاجه جديده علشان دراستي بس بطور مهراتي في القديم" },

  "skills.title": { en: "What I work with", ar: "الأدوات اللي بشتغل بيها" },
  "skills.c1.title": { en: "Development", ar: "البرمجة" },
  "skills.c1.desc": { en: "Building web applications end to end.", ar: "بناء تطبيقات ويب من الصفر للنهاية." },
  "skills.c2.title": { en: "Infrastructure & Networking", ar: "البنية التحتية والشبكات" },
  "skills.c2.desc": { en: "Designing and running the network underneath the app.", ar: "تصميم وتشغيل الشبكة اللي شغالة تحت التطبيق." },
  "skills.c3.title": { en: "Systems & Tools", ar: "الأنظمة والأدوات" },
  "skills.c3.desc": { en: "The hardware and habits behind everything above.", ar: "الهاردوير والعادات اللي وراء كل ده." },

  "process.title": { en: "How a project moves", ar: "رحلة المشروع خطوة بخطوة" },
  "process.p1": { en: "Analyzing project requirements and understanding business needs, then designing the solution and overall system architecture while selecting the appropriate technologies and creating a clear implementation plan.", ar: "تحليل المتطلبات وفهم احتياجات المشروع، ثم تصميم الحل والهيكل العام للنظام مع تحديد التقنيات المناسبة وتجهيز خطة واضحة للتنفيذ." },
  "process.p2": { en: "Converting the design and requirements into a functional system through software development, building components and interfaces, integrating services, and writing organized and scalable code.", ar: "تحويل التصميم والمتطلبات إلى نظام فعلي من خلال تطوير البرمجيات، بناء المكونات والواجهات، تكامل الخدمات، وكتابة كود منظم وقابل للتوسع." },
  "process.p3": { en: "Preparing production environments and deploying applications and services to servers, while configuring networking, systems, containers, and databases to ensure a stable and secure production environment.", ar: "تجهيز بيئة التشغيل ونشر التطبيقات والخدمات على الخوادم، مع إعداد الشبكات والأنظمة والحاويات وقواعد البيانات وضمان استقرار وأمان بيئة الإنتاج." },
  "process.p4": { en: "Monitoring systems and services, identifying and resolving issues, improving performance and security, and performing continuous updates and maintenance to ensure reliability and efficiency.", ar: "مراقبة الأنظمة والخدمات، اكتشاف المشكلات وحلها، تحسين الأداء والأمان، وإجراء التحديثات والصيانة المستمرة لضمان استمرارية وكفاءة النظام." },

  "gallery.title": { en: "A few photos of me", ar: "شوية صور ليا" },
  "gallery.cap1": { en: "", ar: "" },
  "gallery.cap2": { en: "", ar: "" },
  "gallery.cap3": { en: "", ar: "" },
  "gallery.cap4": { en: "", ar: "" },

  "contact.title": { en: "Let's talk", ar: "يلا نتكلم" },
  "contact.lead": {
    en: "I’m passionate about software development and infrastructure, with a focus on turning ideas into practical, reliable, and scalable solutions — from planning and development to deployment, server and network management, and maintenance. If you have a project, an opportunity, an idea, or simply want to discuss programming, Linux, networking, servers, virtualization, or infrastructure, feel free to reach out. I’m always open to connecting, learning, and sharing knowledge.",
    ar: "أنا مهتم بتطوير البرمجيات والبنية التحتية، وبحب أحوّل الأفكار إلى حلول عملية ومستقرة، بدايةً من التخطيط والتطوير، وصولًا إلى النشر وإدارة الخوادم والشبكات والصيانة.لو عندك مشروع، فكرة، فرصة عمل، أو حابب تتكلم عن البرمجة، Linux، Networking، Servers، Virtualization أو Infrastructure، يسعدني نتواصل ونتبادل الأفكار والخبرات."
  },

  "footer.built": { en: "built with HTML, CSS & JS", ar: "مبني بـ HTML و CSS و JS" },

  "photo.slot":  { en: "PHOTO SLOT<br><span>add img/me-hero.jpg</span>", ar: "مكان الصورة<br><span>ضيف img/me-hero.jpg</span>" },
  "photo.slot2": { en: "PHOTO SLOT<br><span>add img/me-about.jpg</span>", ar: "مكان الصورة<br><span>ضيف img/me-about.jpg</span>" },
  "photo.slotg":  { en: "PHOTO SLOT<br><span>img/gallery-1.jpg</span>", ar: "مكان الصورة<br><span>img/gallery-1.jpg</span>" },
  "photo.slotg2": { en: "PHOTO SLOT<br><span>img/gallery-2.jpg</span>", ar: "مكان الصورة<br><span>img/gallery-2.jpg</span>" },
  "photo.slotg3": { en: "PHOTO SLOT<br><span>img/gallery-3.jpg</span>", ar: "مكان الصورة<br><span>img/gallery-3.jpg</span>" },
  "photo.slotg4": { en: "PHOTO SLOT<br><span>img/gallery-4.jpg</span>", ar: "مكان الصورة<br><span>img/gallery-4.jpg</span>" }
};

// =========================================================
// Language handling
// =========================================================
function detectLang() {
  const saved = localStorage.getItem('lang');
  if (saved === 'en' || saved === 'ar') return saved;
  return navigator.language && navigator.language.startsWith('ar') ? 'ar' : 'en';
}

function applyLang(lang) {
  document.documentElement.setAttribute('lang', lang);
  document.documentElement.setAttribute('dir', lang === 'ar' ? 'rtl' : 'ltr');
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    const entry = TRANSLATIONS[key];
    if (entry && entry[lang] != null) {
      el.innerHTML = entry[lang];
    }
  });
  localStorage.setItem('lang', lang);
}

// =========================================================
// Theme handling
// =========================================================
function detectTheme() {
  const saved = localStorage.getItem('theme');
  if (saved === 'light' || saved === 'dark') return saved;
  return window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
}

function applyTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('theme', theme);
}

// init before paint-adjacent logic
applyLang(detectLang());
applyTheme(detectTheme());

document.getElementById('langToggle').addEventListener('click', () => {
  const next = document.documentElement.getAttribute('lang') === 'ar' ? 'en' : 'ar';
  applyLang(next);
});
document.getElementById('themeToggle').addEventListener('click', () => {
  const next = document.documentElement.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
  applyTheme(next);
});

// =========================================================
// Year in footer
// =========================================================
document.getElementById('year').textContent = new Date().getFullYear();

// =========================================================
// Mobile nav toggle
// =========================================================
const navToggle = document.getElementById('navToggle');
const navLinks = document.getElementById('navLinks');
navToggle.addEventListener('click', () => {
  const open = navLinks.classList.toggle('open');
  navToggle.classList.toggle('open', open);
  navToggle.setAttribute('aria-expanded', open);
});
navLinks.querySelectorAll('a').forEach(a => {
  a.addEventListener('click', () => {
    navLinks.classList.remove('open');
    navToggle.classList.remove('open');
  });
});

// =========================================================
// Scroll spy — highlight current nav link
// =========================================================
const sections = document.querySelectorAll('main .section, .hero');
const navItems = document.querySelectorAll('[data-nav]');
const spy = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const id = entry.target.id;
      navItems.forEach(link => {
        link.classList.toggle('active', link.getAttribute('href') === '#' + id);
      });
    }
  });
}, { rootMargin: '-45% 0px -45% 0px' });
sections.forEach(s => spy.observe(s));

// =========================================================
// Scroll reveal
// =========================================================
const revealEls = document.querySelectorAll('.reveal');
const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('in');
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.15 });
revealEls.forEach(el => revealObserver.observe(el));

const processLine = document.querySelector('.process-line');
if (processLine) {
  const po = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { processLine.classList.add('in'); po.disconnect(); } });
  }, { threshold: 0.3 });
  po.observe(processLine);
}

// =========================================================
// Cursor glow
// =========================================================
const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
const glow = document.getElementById('cursorGlow');
if (!reduceMotion && window.matchMedia('(pointer:fine)').matches) {
  window.addEventListener('mousemove', (e) => {
    glow.style.transform = `translate(${e.clientX}px, ${e.clientY}px) translate(-50%,-50%)`;
  });
}

// =========================================================
// Hero photo tilt
// =========================================================
const photoFrame = document.querySelector('.hero-photo-frame');
if (photoFrame && !reduceMotion) {
  const heroSection = document.querySelector('.hero');
  heroSection.addEventListener('mousemove', (e) => {
    const rect = heroSection.getBoundingClientRect();
    const px = (e.clientX - rect.left) / rect.width - 0.5;
    const py = (e.clientY - rect.top) / rect.height - 0.5;
    photoFrame.style.transform = `rotateY(${px * 10}deg) rotateX(${-py * 10}deg)`;
  });
  heroSection.addEventListener('mouseleave', () => {
    photoFrame.style.transform = 'rotateY(0) rotateX(0)';
  });
}

document.querySelectorAll('.gallery-item, .about-photo-frame').forEach(card => {
  if (reduceMotion) return;
  card.addEventListener('mousemove', (e) => {
    const rect = card.getBoundingClientRect();
    const px = (e.clientX - rect.left) / rect.width - 0.5;
    const py = (e.clientY - rect.top) / rect.height - 0.5;
    card.style.transform = `translateY(-4px) rotateY(${px * 8}deg) rotateX(${-py * 8}deg)`;
  });
  card.addEventListener('mouseleave', () => { card.style.transform = ''; });
});

// =========================================================
// Network topology canvas — theme-aware colors
// =========================================================
(function topology() {
  const canvas = document.getElementById('topology');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  let w, h, dpr;
  let nodes = [];
  const NODE_COUNT_BASE = 46;
  const mouse = { x: -9999, y: -9999 };

  function themeColors() {
    const cs = getComputedStyle(document.documentElement);
    return {
      line: cs.getPropertyValue('--topology-line').trim() || '139,150,166',
      amber: cs.getPropertyValue('--topology-amber').trim() || '232,163,61',
      teal: cs.getPropertyValue('--topology-teal').trim() || '79,216,196'
    };
  }

  function resize() {
    dpr = Math.min(window.devicePixelRatio || 1, 2);
    w = canvas.offsetWidth;
    h = canvas.offsetHeight;
    canvas.width = w * dpr;
    canvas.height = h * dpr;
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    seedNodes();
  }

  function seedNodes() {
    const count = Math.min(NODE_COUNT_BASE, Math.floor((w * h) / 26000));
    nodes = Array.from({ length: Math.max(count, 18) }, () => ({
      x: Math.random() * w,
      y: Math.random() * h,
      vx: (Math.random() - 0.5) * 0.18,
      vy: (Math.random() - 0.5) * 0.18,
      r: Math.random() < 0.18 ? 3.4 : 1.8,
      kind: Math.random() < 0.5 ? 'amber' : 'teal'
    }));
  }

  function step() {
    const colors = themeColors();
    ctx.clearRect(0, 0, w, h);

    nodes.forEach(n => {
      n.x += n.vx; n.y += n.vy;
      if (n.x < 0 || n.x > w) n.vx *= -1;
      if (n.y < 0 || n.y > h) n.vy *= -1;
    });

    const maxDist = Math.min(150, w / 5);
    for (let i = 0; i < nodes.length; i++) {
      for (let j = i + 1; j < nodes.length; j++) {
        const a = nodes[i], b = nodes[j];
        const dx = a.x - b.x, dy = a.y - b.y;
        const dist = Math.sqrt(dx * dx + dy * dy);
        if (dist < maxDist) {
          const alpha = (1 - dist / maxDist) * 0.18;
          ctx.strokeStyle = `rgba(${colors.line},${alpha})`;
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.moveTo(a.x, a.y);
          ctx.lineTo(b.x, b.y);
          ctx.stroke();
        }
      }
      const dxm = nodes[i].x - mouse.x, dym = nodes[i].y - mouse.y;
      const dm = Math.sqrt(dxm * dxm + dym * dym);
      if (dm < 180) {
        ctx.strokeStyle = `rgba(${colors.amber},${(1 - dm / 180) * 0.4})`;
        ctx.beginPath();
        ctx.moveTo(nodes[i].x, nodes[i].y);
        ctx.lineTo(mouse.x, mouse.y);
        ctx.stroke();
      }
    }

    nodes.forEach(n => {
      ctx.beginPath();
      ctx.fillStyle = n.kind === 'amber' ? `rgba(${colors.amber},0.6)` : `rgba(${colors.teal},0.6)`;
      ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
      ctx.fill();
    });

    requestAnimationFrame(step);
  }

  window.addEventListener('resize', resize);
  canvas.addEventListener('mousemove', (e) => {
    const rect = canvas.getBoundingClientRect();
    mouse.x = e.clientX - rect.left;
    mouse.y = e.clientY - rect.top;
  });
  canvas.addEventListener('mouseleave', () => { mouse.x = -9999; mouse.y = -9999; });

  resize();
  if (!reduceMotion) {
    requestAnimationFrame(step);
  } else {
    step();
  }
})();
